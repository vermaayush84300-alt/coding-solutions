#include <bits/stdc++.h>
using namespace std;

int main() {
	// your code goes here
int t;
cin>>t;
while(t--){
    int n;
    cin>>n;
    vector<long long>arr(n);
    for(int i=0; i<n; i++){
        cin>>arr[i];
    }
    long long ans =0 , priv=0;
    for(int i=0; i<n ; i++){
        long long corrent;
        if(i%2==0){
            corrent=arr[i];
        }
        else{
            corrent = -arr[i];
            
        }
        ans += abs(corrent - priv);
        priv= corrent;
    }
    ans += abs(previous);
    cout<<ans/2<<endl;
}
}
